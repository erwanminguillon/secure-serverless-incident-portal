{
  "name": "secure-serverless-incident-portal",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "src/frontend",
    "src/api"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:api": "npm run dev --workspace api",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces"
  },
  "dependencies": {
    "react-router-dom": "^7.18.2"
  }
}
